<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <TextView
        android:id="@+id/textItemName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="16sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Receive Type"
        android:textSize="10sp"
        android:textColor="#888888"
        android:layout_marginTop="12dp" />

    <RadioGroup
        android:id="@+id/radioReceiveType"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <RadioButton
            android:id="@+id/radioCases"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Cases"
            android:checked="true" />
        <RadioButton
            android:id="@+id/radioBottles"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Bottles" />
    </RadioGroup>

    <TextView
        android:id="@+id/textQtyLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cases"
        android:textSize="10sp"
        android:textColor="#888888"
        android:layout_marginTop="8dp" />
    <EditText
        android:id="@+id/editQty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:text="1" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="8dp">
        <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:orientation="vertical" android:layout_marginEnd="4dp">
            <!-- Label/meaning switches with Receive Type: in Cases mode this is the CASE cost
                 printed on the invoice, and Unit Cost gets computed by dividing it by Units Per
                 Case below - never a number the user has to pre-divide by hand. In Bottles mode
                 it's the per-bottle cost directly, same as before. -->
            <TextView android:id="@+id/textUnitCostLabel" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Case Cost ($)" android:textSize="10sp" android:textColor="#888888" />
            <EditText android:id="@+id/editUnitCost" android:layout_width="match_parent" android:layout_height="wrap_content" android:inputType="numberDecimal" />
        </LinearLayout>
        <LinearLayout android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1" android:orientation="vertical">
            <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Sell Price ($)" android:textSize="10sp" android:textColor="#888888" />
            <EditText android:id="@+id/editPrice" android:layout_width="match_parent" android:layout_height="wrap_content" android:inputType="numberDecimal" />
        </LinearLayout>
    </LinearLayout>

    <!-- Cases mode only - hidden entirely in Bottles mode, since unitsPerCase is pinned to 1 there
         and doesn't apply. Editable because the catalog's own value has been wrong often enough
         this session to need a quick on-the-spot correction. -->
    <LinearLayout
        android:id="@+id/unitsPerCaseContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="8dp">
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Units Per Case" android:textSize="10sp" android:textColor="#888888" />
        <EditText android:id="@+id/editUnitsPerCase" android:layout_width="match_parent" android:layout_height="wrap_content" android:inputType="number" />
        <TextView
            android:id="@+id/textComputedUnitCost"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:textColor="@color/purple_primary"
            android:layout_marginTop="4dp" />
    </LinearLayout>

</LinearLayout>
