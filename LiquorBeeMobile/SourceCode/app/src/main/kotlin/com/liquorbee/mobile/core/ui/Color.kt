package com.liquorbee.mobile.core.ui

import androidx.compose.ui.graphics.Color

// Sourced directly from the web app's palette (home-page.html tile colors, nav-menu.component.html
// navbar background) so the native app reads as the same product.
val LiquorBeeNavy = Color(0xFF030F2B)
val LiquorBeeNavyLight = Color(0xFF0F1E4A)
val LiquorBeeBlue = Color(0xFF3BB2FF)
val LiquorBeeSurface = Color(0xFFF5F5F7)
val LiquorBeeError = Color(0xFFD32F2F)
